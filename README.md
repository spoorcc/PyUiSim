# PySimUI

This repository is a proof-of-concept of creating a python based Ui from a single svg file.

## Usage

```
python -m venv
. venv/bin/activate
python -m pip install -r requirements.txt
python pyuisim.py
```